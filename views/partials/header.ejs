<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>FurEverHome</title>
    <link rel="stylesheet" href="/css/main.css" />
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH"
      crossorigin="anonymous"
    />
  </head>
  <body>
    <header>
      <nav class="navbar navbar-expand-md navbar-dark fixed-top bg-dark">
        <div class="container-fluid">
          <div class="navbar-left">
            <a class="navbar-brand" href="/">FurEverHome</a>
            <ul class="navbar-nav mr-auto">
              <li class="nav-item active">
                <a class="nav-link" href="/">Home</a>
              </li>
            </ul>
          </div>
          <div class="navbar-right">
            <ul class="navbar-nav ml-auto">
              <% if (user) { %>
              <form
                class="form-inline mt-2 mt-md-0 mr-3"
                action="/"
                method="GET"
              >
                <input
                  class="form-control mr-sm-2"
                  type="text"
                  placeholder="Search by type"
                  aria-label="Search"
                  name="search"
                  value="<%= typeof search !== 'undefined' ? search : '' %>"
                />
                <button
                  class="btn btn-outline-success my-2 my-sm-0"
                  type="submit"
                >
                  Search
                </button>
              </form>
              <li class="nav-item">
                <a class="nav-link border-light" href="/favorites">⭐️ Fav</a>
              </li>
              <% if (currentUrl === '/pets/yourPets') { %>
              <li class="nav-item">
                <a class="nav-link border-light" href="/pets/new">Add Pet</a>
              </li>
              <% } else { %>
              <li class="nav-item">
                <a class="nav-link border-light" href="/pets/yourPets"
                  >Your Pets</a
                >
              </li>
              <% } %> <% if (user.isAdmin) { %>
              <li class="nav-item">
                <a class="nav-link border-light" href="/users">All Users</a>
              </li>
              <% } %>
              <li class="nav-item">
                <a class="nav-link border-light" href="/auth/sign-out"
                  >SIGN OUT</a
                >
              </li>
              <li class="nav-item">
                <span class="navbar-text">Welcome <%= user.name %></span>
              </li>
              <% } else { %> <% if (currentUrl === '/auth/sign-up') { %>
              <li class="nav-item">
                <a class="nav-link border-light" href="/auth/sign-in"
                  >SIGN IN</a
                >
              </li>
              <% } else if (currentUrl === '/auth/sign-in') { %>
              <li class="nav-item">
                <a class="nav-link border-light" href="/auth/sign-up"
                  >SIGN UP</a
                >
              </li>
              <% } else { %>
              <li class="nav-item">
                <a class="nav-link border-light" href="/auth/sign-up"
                  >SIGN UP</a
                >
              </li>
              <li class="nav-item">
                <a class="nav-link border-light" href="/auth/sign-in"
                  >SIGN IN</a
                >
              </li>
              <% } %> <% } %>
            </ul>
          </div>
        </div>
      </nav>
    </header>
    <main role="main" class="container mt-5">
